// Prisma artifacts removed. The original schema and migrations were related
// to the previous MySQL/Prisma setup and have been intentionally cleared.
// If you need to restore them, retrieve from git history.